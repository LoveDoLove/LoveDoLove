name: Sync Top Starred Projects

permissions:
  contents: write

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  sync:
    uses: LoveDoLove/Github-Forks-Sync-Manager/.github/workflows/sync-top-starred.yml@main
